# Packages

| Package | Role |
| --- | --- |
| [`imp-spec`](./imp-spec/) | Core graph type interfaces (regenerable from `docs/features/graph-model.md`) |

Future packages will mostly be converters/translators (e.g. Imp ↔ React Flow). Each package should have a `README.md` (human context) and `AGENTS.md` (how to work in the package).
